# nnz-mvp 2026-06-05 工作记录与交接

> 写给下一个 AI / Claude Code / Codex：这份记录说明今天完成了什么、遇到了哪些坑、当前代码处于什么状态、下一步计划是什么。

## 当前状态

**45 条测试全绿，build 通过。DeepSeek V4 Pro 已接入对话生成和提取管线。**

## 今天完成的事项

### 1. 部署到 Render（续昨）

- 发现并修复 `dist-cjs/package.json` 缺失问题（`build:demo` 脚本加自动创建步骤）
- 修复 git push 的 PAT 格式问题：必须用 `https://x-access-token:TOKEN@github.com/...`，不能用 `TOKEN@github.com`
- 推送后 GitHub 拦截了硬编码 token → 替换为 `YOUR_GITHUB_PAT` 占位符
- Render URL：`https://nnz-kego.onrender.com`，6/6 PASS

### 2. LLM 调用基础设施

新建文件：

| 文件 | 职责 |
|:---|:---|
| `src/env.ts` | `.env` 自动加载（零外部依赖） |
| `src/llm/types.ts` | 请求/响应接口 |
| `src/llm/adapter.ts` | OpenAI-compatible adapter + mock adapter + `createAdapterFromEnv()` |
| `src/llm/adapter.test.ts` | 4 条 mock 测试 |

安全边界：
- API key 只从 `process.env.NNZ_LLM_API_KEY` 读取
- `.env` 已被 `.gitignore` 排除
- 错误信息不暴露 key
- Mock adapter 让测试无需真实 API

.env 配置（DeepSeek V4 Pro）：
```
NNZ_LLM_API_KEY=sk-xxx
NNZ_LLM_BASE_URL=https://api.deepseek.com/v1
NNZ_LLM_MODEL=deepseek-v4-pro
```

### 3. 自动化提取管线

新建文件：

| 文件 | 职责 |
|:---|:---|
| `src/extraction/types.ts` | ExtractionResult / MergedField / SOUL_FIELD_MAP |
| `src/extraction/prompts.ts` | 提取用 system prompt + 对话窗口构造 |
| `src/extraction/confidence.ts` | 确定性置信度计算（LLM 置信度为基 + 证据数/一致性/用户描述加权） |
| `src/extraction/orchestrator.ts` | 编排器：触发判断 → LLM 调用 → 合并 → 生成 Proposal |
| `src/extraction/orchestrator.test.ts` | 4 条测试 |

**触发条件**：每累计 5 条新对话触发一次提取，取最近 10 轮送入 LLM。

**置信度规则**：
- LLM 返回的 confidence 为基
- ≥3 条 evidence → +0.2
- 与已有高置信度记忆一致 → +0.1
- 用户曾明确描述过该特征 → +0.3（SOURCE_WEIGHT_USER_INPUT 补偿）
- 已有 PENDING proposal 同 fieldPath → 跳过
- ≥0.7 → 生成 Proposal，<0.7 → 只写 Memory

### 4. LLM 对话生成（今天重点）

`generateLlmReply()` 直接生成聊天回复，替代原有的确定性 `generateSoulReply`。

**System prompt 注入**：
- Soul 的称呼、关系、幽默度、口头禅
- 相关 Memory 摘要
- 知识截止日期（如有）
- 禁止暴露机制词、禁止情景描述、禁止替用户做决定

**Fallback**：LLM 回复如果包含机制泄漏词，自动回退到确定性生成。

**UI 改进**：
- 发送时按钮变灰「发送中...」+ 脉冲动画「...思考中」
- 防重复点击（sending 标志）
- 回复到达后自动恢复

### 5. 连续相同消息去重

用户短时间内发相同消息（追问）时，不重复调 LLM，直接复用上次回复。

逻辑：`sendMessageToBothUsers` 在添加新的 USER 消息**之前**检查上一条用户消息是否内容相同。相同则取上一条 assistant 回复直接返回。

## 今天遇到的坑

| 问题 | 原因 | 修复 |
|:---|:---|:---|
| A/B 回复完全一样 | LLM 没接入对话，仍用旧的确定性 `generateSoulReply` | 新增 `generateLlmReply`，替换对话生成路径 |
| 发送消息后无回复 | `sendMessageToBothUsers` 改为 async 后调用处没加 `await`，`sendJson` 收到 Promise 对象 | 加 `await` |
| 所有回复都是「（嗯，我听见了。）」 | `isDuplicateMessage` 在写入用户消息**之后**检查，每次都命中刚写入的自己 | 调顺序：先检查重复，再写入 |
| 回复中出现「（放下手中的报纸，摘下老花镜）」 | DeepSeek 倾向生成叙事性描述 | prompt 加规则禁止 + 正则后处理清除 |
| `.env` 不自动加载 | Node 不会自动读取 `.env` | 新建 `src/env.ts`，`demo-server.ts` 启动时调 `loadEnv()` |
| git push 失败 | PAT URL 格式错误（缺 `x-access-token:` 前缀） | 改为 `https://x-access-token:TOKEN@github.com/...` |
| GitHub 拦截推送（含明文 token） | 文档中硬编码了 PAT | 替换为 `YOUR_GITHUB_PAT` 占位符 |
| Render 首次部署失败 | 旧代码无 `start` 脚本 | 推送含 `start` 脚本的新代码后自动重试成功 |

## 当前代码结构

```
src/
├── env.ts                    ← NEW .env 加载器
├── llm/
│   ├── types.ts              ← NEW 请求/响应接口
│   ├── adapter.ts            ← NEW OpenAI adapter + mock
│   └── adapter.test.ts       ← NEW 4 tests
├── extraction/
│   ├── types.ts              ← NEW 提取结果类型
│   ├── prompts.ts            ← NEW 提取 prompt
│   ├── confidence.ts         ← NEW 置信度计算
│   ├── orchestrator.ts       ← NEW 编排器
│   └── orchestrator.test.ts  ← NEW 4 tests
├── domain/
│   ├── types.ts
│   ├── errors.ts
│   ├── soul-store.ts
│   ├── soul-scope.test.ts    (19 tests)
│   └── index.ts
├── runtime/
│   ├── soul-runtime.ts
│   ├── soul-guard.ts
│   ├── soul-runtime.test.ts  (4 tests)
│   └── soul-guard.test.ts    (14 tests)
└── demo-server.ts            ← CHANGED: LLM chat + extraction hook + loading UI + dedup
```


---

## 晚间补充：Prompt 重构 — A/B 回复差异

### 问题

用户发现 A 和 B 的 LLM 回复趋同。根因分析后发现 `generateLlmReply` 有三个设计缺陷：

1. **无对话历史** — LLM 每次只看到孤立的单条消息，不感知上下文
2. **人格描述扁平** — `humorLevel=high` 只是一个词，缺少行为展开
3. **关系只是字符串** — "女儿心中的父亲" 没有转化成具体的说话风格指引
4. **口头禅未展开** — 只取了第一个 petPhrase（`readFirstString`），丢了列表中的其余口头禅

### 修复

Prompt 重构为五层分化：

| 层级 | 内容 | 效果 |
|:---|:---|:---|
| [WHO YOU ARE] | 该用户的 DESCRIPTION + CORRECTION 记忆全文 | A: "爸爸其实很幽默，只是不太主动开玩笑" vs B: 无此记忆 → 不同的幽默表达 |
| [关系风格] | 根据 relationship 生成行为指引 | A（女儿）→ tender concern, protective; B（儿子）→ steady encouragement, independence |
| [对话历史] | 最近 6 轮对话作为 user prompt 前缀 | A 聊婚礼 → 回复偏温情; B 聊工作 → 回复偏鼓励 |
| [幽默度] | 展开为行为描述 | high→"often cracks jokes, respects boundaries"; low→"speaks plainly, shows care through actions" |
| [口头禅] | 完整列表，要求自然融入 | A: "你自己拿主意"; B: "慢慢来" |

### 相关代码变更

- `generateLlmReply` 新增第 4 个参数 `recentConversations`
- 新增 `readStringArray` 辅助函数（读取完整口头禅列表）
- `sendMessageToBothUsers` 的调用处传入 `fixture.store.listConversations(scope)`

### 构建验证

build 通过，45 条测试全绿。

## 下一步计划

### 立即：修 A/B 回复相同

当前 `generateLlmReply` 虽然给 A 和 B 分别注入了各自的 Soul 信息（不同的称呼、关系、幽默度、口头禅），但实际测试中 A/B 回复仍然可能相近。需要在 `sendMessageToBothUsers` 中分别调用 LLM 时确认参数确实是不同的。

### 短期：对话上下文

当前 LLM 每次只看到孤立的单条消息，没有对话历史。需要把最近 3-5 轮对话注入 prompt，让回复更连贯。

### 中期：推送到 GitHub + Render

本地改动未提交（因沙箱 git push 有 30s 超时）。需要用 `x-access-token` 格式推送。

## 环境说明

### 本 AI 的工作环境（Codex 沙箱）

- 文件系统可读写 iCloud/Obsidian 目录
- npm install 需要 escalated 权限（网络受限）
- 进程有 30 秒硬限制（git push / tsc 可能超时）
- 沙箱拒绝监听端口（EPERM on 0.0.0.0）
- 另一个 AI（Claude Code）在不同环境中工作

### 跨 AI 协作注意事项

1. **Obsidian 知识库是本地的**：`/Users/will/Library/Mobile Documents/iCloud~md~obsidian/Documents/黑曜石知识库/Personal/我还在/` 下的 `.md` 文件是 Obsidian vault。另一个 AI 通过读取这些文件获取上下文。
2. **GitHub 是公用的**：`https://github.com/chaoshorizon316/nnz`，CI 自动验证。另一个 AI 可以 clone 来获得完整代码。
3. **Render 是公用的**：`https://nnz-kego.onrender.com`，在线 demo。
4. **API key 在 `.env` 中**：不提交到 Git。另一个 AI 如果需要调 LLM，需要自己获取 API key 或用户提供。
