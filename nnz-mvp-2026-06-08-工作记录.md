# nnz-mvp 2026-06-08 工作记录与交接

> 写给下一个 AI：这是今天完成的所有事项、当前代码状态、下一步计划。

> 2026-06-09 纠偏：本文件记录的是 6 月 8 日当时的工作意图和阶段状态。随后发现该批变更导致 CI failure，并存在 credential 类型/作用域问题。已在 `nnz-mvp-2026-06-09-工作记录.md` 修复并重新验证为 61 tests passed、typecheck/build/audit 通过。

## 当前状态

**Git 已同步**：`main...origin/main`，5 个 commit 全部推送。Render 自动部署中。

**Render URL**：`https://nnz-kego.onrender.com`

## 今天完成

### 1. SQLite 持久化（续昨）

新增 `src/domain/persistence.ts` — `saveStore()` / `loadStore()` 两个函数。`InMemorySoulStore` 加了 `serialize()` / `deserialize()` 方法。

- 55 条测试全绿（含 3 条持久化测试）
- 在 `.env` 里加 `NNZ_DB_PATH=./nnz.db` 即可启用
- Render 免费版不支持持久文件，生产需换 Render PostgreSQL

### 2. 登录/注册

新建 `src/auth/auth.ts`：
- bcrypt 密码哈希
- JWT 签发/验证（7 天有效期）
- Bearer token 提取

新增端点：
- `POST /api/register` — email + password → JWT
- `POST /api/login` — email + password → JWT

前端：页面顶部有登录/注册表单，登录状态存 localStorage

### 3. 官网首页落地

把 `index.html`（品牌落地页）搬到 `nnz-mvp/public/`，Render 直接服务。

| 路径 | 内容 |
|:---|:---|
| `/` | 品牌落地页（Hero、核心价值、伦理承诺、定价、User Stories） |
| `/styles.css` | 首页样式 |
| `/demo` | Chat Demo（双用户并排聊天 + Soul Ops Console） |
| `/api/*` | 所有 API |

首页「在线体验」区块用 iframe 嵌入 `/demo`（同域）。

### 4. 构建流程更新

`build:demo` 脚本加了一步：编译后自动复制 `public/` 到 `dist-cjs/public/`。

## 当前架构

```
nnz-kego.onrender.com
├── /                    ← 品牌官网（index.html + styles.css）
│   └── iframe → /demo   ← 在线体验
├── /demo                ← Chat Demo（开发者页面）
├── /api/register        ← 注册
├── /api/login           ← 登录
├── /api/chat            ← 聊天（LLM 生成）
├── /api/state           ← 完整状态
├── /api/verification    ← 验证检查
├── /api/seal            ← 封存
├── /api/activate-node   ← 节点重启
├── /api/complete-node   ← 完成节点
├── /api/graduate        ← 毕业
├── /api/reset           ← 重置
└── /api/run-all         ← 一键验证
```

## 下一步（给另一个 AI）

### 优先级 1：把登录对接真实用户数据

当前登录后仍使用 A/B demo fixture。需要把认证用户映射到真实的 Soul 数据：
- 登录后根据 userId 查询该用户的 Persona 列表
- Chat 使用用户自己的 Soul + Memory
- 不再显示"A 用户/B 用户"标签

### 优先级 2：微信接入

产品设计的核心交互载体。需要：
- 企业微信 API 或微信对话开放平台
- 消息收发 + 自动回复
- 用户引导对话流

### 优先级 3：官网内容完善

`public/index.html` 里的定价、用户故事、常见问题等内容目前是占位文案，需要替换为真实内容。

## 环境注意事项

- `.env` 文件在 `nnz-mvp/` 下，包含 LLM API key（已被 .gitignore 排除）
- Render 环境变量已在 Dashboard 配置（`NNZ_LLM_API_KEY` 等）
- iCloud 目录下 `node_modules` 偶发缺依赖文件，`npm test` 可能误报失败
- 可靠验证方式：复制到 `/tmp` 后 `npm ci && npm test`
- Git push 在沙箱中可能超时，用户需在终端手动推送
