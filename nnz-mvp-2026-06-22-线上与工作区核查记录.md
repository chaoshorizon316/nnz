# nnz-mvp 2026-06-22：线上与工作区核查记录

## Summary

本次核查目标：确认线上环境状态，并检查另一位 AI 在 2026-06-18 至 2026-06-21 期间做了什么、为什么问题没有完全修复。

结论：

- Render 线上服务仍可用。
- 线上 `/healthz` 正常返回 Postgres 持久化状态。
- Soul Ops audit API 的无 token 鉴权边界正常返回 401。
- GitHub `main` 最新提交为 `560520f fix: revert index.html to stable version before modal`。
- 最新 GitHub Actions `NNZ MVP CI` run `27902512627` 成功。
- 另一位 AI 主要反复修改 `nnz-mvp/public/index.html` 的 H5/modal/CTA 行为，最后回退到 modal 前的稳定版。
- 当前 Codex workspace 指向的 `黑曜石知识库/Personal/我还在` 是异常空壳副本，不是当前完整工作区。
- 当前完整工作区位于 `黑曜石知识库 2/Personal/我还在`。

## Online Checks

Render URL：

```text
https://nnz-kego.onrender.com
```

### Health Check

命令：

```bash
curl -sS -i https://nnz-kego.onrender.com/healthz
```

返回要点：

```json
{
  "ok": true,
  "service": "nnz-mvp-demo",
  "fixture": "postgres",
  "persistence": {
    "mode": "postgres",
    "postgresConfigured": true,
    "postgresEnv": "DATABASE_URL",
    "sqliteConfigured": false
  }
}
```

判断：线上 demo 存活，且仍使用 Render Postgres 持久化。

### Soul Ops Audit API Auth Boundary

命令：

```bash
curl -sS -i https://nnz-kego.onrender.com/api/ops/audit-events
```

返回：

```text
HTTP/2 401
{
  "error": "缺少 Soul Ops 访问 token。"
}
```

判断：无 token 访问仍被拒绝，鉴权边界正常。本次没有读取、请求或记录任何后台 token 明文。

### Homepage HTML

线上首页 HTML 确认：

- 存在内嵌 H5 demo 区。
- CTA 当前滚动到 `#demo`。
- 当前没有 `openExperience()` modal 入口。
- 存在 `h5GuestMode()`、`h5NextPage()`、三步创建体验等 H5 逻辑。

判断：线上当前不是 modal 体验，而是回退到 modal 前的稳定内嵌 H5 体验。

## GitHub / CI Status

仓库：

```text
https://github.com/chaoshorizon316/nnz
```

最新提交：

```text
560520f fix: revert index.html to stable version before modal
```

最新 GitHub Actions：

```text
Workflow: NNZ MVP CI
Run: 27902512627
Head SHA: 560520f25b784130d1824736c669cded7b7f5ca2
Status: completed
Conclusion: success
Created: 2026-06-21T11:14:11Z
Updated: 2026-06-21T11:14:34Z
```

## What The Other AI Changed

另一位 AI 的工作集中在首页 H5 / modal / CTA，不是 Step 2.5 scoped repository。

近期提交脉络：

```text
68e89c0 feat: guided 3-step personality creation with trait selection cards
0ba8331 fix: CTA buttons scroll to demo section instead of broken modal
37a405e fix: Step 1 guest mode + Step 2 input spacing
83e2e72 feat: move H5 experience to modal dialog + cache-busting meta tags
608c2d3 Revert "feat: move H5 experience to modal dialog + cache-busting meta tags"
276dc61 feat: modal wrapper for H5 experience using CSS overlay
d9bf8ee fix: CTA buttons now call openExperience() for modal
4cf7484 fix: ensure modal code is deployed with version tag
c438fc8 fix: simplify modal trigger - no scroll
a227209 fix: use inline styles instead of CSS classes for modal to avoid Tailwind conflicts
98aebc8 fix: move modal JS to main script block, fix h5NextPage null checks
560520f fix: revert index.html to stable version before modal
```

观察：

- 多次尝试把 H5 体验改成 modal。
- 多次修复 CTA、modal JS、Tailwind 冲突和 null checks。
- 最后通过 `560520f` 回退到 modal 前稳定版本。
- 未见 2026-06-19 或 2026-06-21 对应的本地知识库工作记录。

## Local Workspace Finding

当前 Codex 环境给出的 workspace：

```text
/Users/will/Library/Mobile Documents/iCloud~md~obsidian/Documents/黑曜石知识库/Personal/我还在
```

异常状态：

- 目录几乎为空。
- `.git` 不是完整 Git 仓库，只剩 `fsmonitor--daemon.ipc`。
- `nnz-mvp` 目录基本只剩 `node_modules`。
- 无法运行 `git status`，报 `fatal: not a git repository`。
- 原知识库文档和源码不在这份目录中。

找到的完整副本：

```text
/Users/will/Library/Mobile Documents/iCloud~md~obsidian/Documents/黑曜石知识库 2/Personal/我还在
```

状态：

```text
git status -sb -> ## main...origin/main
HEAD -> 560520f
工作区干净
```

另一个旧副本：

```text
/Users/will/Library/Mobile Documents/iCloud~md~obsidian/Documents/黑曜石知识库 3/Personal/我还在
```

状态：较旧，约停在 2026-06-04 附近，不应作为当前主线。

## Risk Assessment

当前最大风险不是线上服务宕机，而是本地路径混乱：

- Codex 当前默认 workspace 指向空壳副本。
- 完整代码和知识库在 `黑曜石知识库 2`。
- 如果继续在空壳副本中操作，可能导致误判、误删或遗漏日志。

另一个风险是流程问题：

- 另一位 AI 的提交进入 GitHub，但未按约定沉淀每日关键进展到 Obsidian 工作记录。
- 后续接手者只能从 commit history 反推上下文，增加恢复成本。

## Recommended Next Steps

1. 将 `黑曜石知识库 2/Personal/我还在` 确认为当前真实工作区。
2. 暂停在 `黑曜石知识库/Personal/我还在` 空壳副本中做源码修改。
3. 视用户偏好，将空壳副本从完整副本恢复，或调整 Codex workspace 指向完整副本。
4. 再决定是否继续修 H5 modal；若目标是稳定可用，当前线上内嵌 H5 可作为稳定基线。
5. 工程主线仍建议回到 Step 2.5：Postgres scoped repository 设计与最小落地。
6. 每日/每阶段继续沉淀：
   - `nnz-mvp-YYYY-MM-DD-*.md`
   - `nnz-mvp-CURRENT-STATE.md`
   - `CLAUDE_CODE_HANDOFF.md`
   - 必要时更新 `nnz-mvp/CLAUDE_CODE_HANDOFF.md` 与 `nnz-mvp/README.md`

## Scope / Ethics Audit

本次没有改动用户端机制文案、LLM prompt、Soul Runtime、Memory retrieval、Soul Ops 权限或数据删除逻辑。

保持：

- 用户端不暴露 `SoulVersion` / `MemoryItem` / `SoulUpdateProposal` / `userId + personaId` 等机制。
- Soul / Memory / Snapshot / Node / Conversation 的最高优先级边界仍是 `userId + personaId`。
- 未读取、记录或请求任何后台 token 明文。
