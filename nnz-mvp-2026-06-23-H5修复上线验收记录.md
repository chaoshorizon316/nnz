# nnz-mvp 2026-06-23 H5 修复上线验收记录

## Summary

本次目标：将 2026-06-22 / 2026-06-23 已完成的 H5 modal、首页 CTA、创建体验优化提交到 GitHub，并确认 GitHub Actions 与 Render 线上环境均已生效。

结论：

- 已提交并推送到 GitHub `main`。
- 最新提交：`5e0df09 fix: restore h5 experience modal`。
- GitHub Actions `NNZ MVP CI` run `28012032867` 已完成，结论为 success。
- Render `/healthz` 返回 200，线上仍使用 Postgres fixture。
- Render 首页 HTML 已包含 H5 modal、`openExperience(event)` CTA、默认 hidden guard、以及特征 checkbox 多选 UI。

## Local Commit / Push

提交：

```text
5e0df09 fix: restore h5 experience modal
```

本地与远端确认：

```text
git status -sb: ## main...origin/main
HEAD:        5e0df098bd31285a8a50ca5dc8d4f769cb19cecf
origin/main: 5e0df098bd31285a8a50ca5dc8d4f769cb19cecf
origin/HEAD: 5e0df09
```

说明：终端 `git push origin main` 因当前 HTTPS 凭据不可读失败；随后使用 GitHub Desktop 已登录会话完成 `Push origin`，并回到终端确认 `origin/main` 与 `HEAD` 一致。

## Pre-push Verification

提交前在完整主库执行：

```text
git diff --check: passed
node inline script syntax: script-ok
npm run typecheck: passed
npm test: passed, 12 test files / 79 tests
npm run build:demo: passed
npm audit: found 0 vulnerabilities
local /healthz smoke: passed, sqlite fixture
local homepage smoke: confirmed modal hidden guard, openExperience CTA, trait-check checkbox UI
```

## GitHub Actions

CI run：

```text
https://github.com/chaoshorizon316/nnz/actions/runs/28012032867
```

API 返回要点：

```text
workflow: NNZ MVP CI
head_sha: 5e0df098bd31285a8a50ca5dc8d4f769cb19cecf
status: completed
conclusion: success
created_at: 2026-06-23T08:11:40Z
updated_at: 2026-06-23T08:12:06Z
```

## Render Smoke

Render URL：

```text
https://nnz-kego.onrender.com
```

`/healthz` 返回：

```json
{
  "ok": true,
  "service": "nnz-mvp-demo",
  "fixture": "postgres",
  "persistence": {
    "mode": "postgres",
    "postgresConfigured": true,
    "postgresEnv": "DATABASE_URL",
    "sqliteConfigured": false
  }
}
```

首页 HTML smoke 确认包含：

```text
.nnz-experience-modal
.nnz-experience-modal[hidden]
openExperience(event)
trait-check
h5-trait-options
```

判断：线上首页已经从旧的内嵌滚动 H5 基线切到本次修复后的 H5 modal 体验。

## Notes

- 本次上线没有改动 Soul / Memory / Snapshot / Node / Conversation 的作用域模型。
- 本次上线没有改动 Soul Ops token、RBAC、审计日志、删除回执或云端 Postgres 配置。
- 用户端仍不暴露 `userId`、`personaId`、`SoulVersion`、`MemoryItem`、`SoulUpdateProposal` 等后台机制词。
- 后续工程主线建议回到 Step 2.5：Postgres scoped repository 设计与最小落地。
